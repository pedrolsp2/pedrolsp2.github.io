use toplay;

create table prereserva(
DataPartida datetime,
HorarioPartida time,
DuracaoPartida time,
NumeroJogadores int ,
NumeroBolas int,
NumeroUniformes int,
NomeJogador varchar(255),
TelJogador varchar(255)
);